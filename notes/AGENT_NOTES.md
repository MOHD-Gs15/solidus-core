# AGENT NOTES — Solidus 2.1.4 Implementation Notes

> ملاحظات منفصلة من الوكيل المنفّذ — للمراجعة التقنية وقرارات التصميم
> والمخاطر المتبقية واختبارات القبول المقترحة. ليست بديلاً عن
> `docs/FEATURES_TRADE_BIDDING.md` (التوثيق الرسمي) بل مكمّلة له.

---

## 1) ملخص سريع لما تم

| المهمة | الحالة |
| --- | --- |
| الإضافة 1 — المزايدة (Bidding) للمزاد | ✅ منفّذة بالكامل (escrow + anti-snipe + استرداد ذاتي + GUI + أوامر) |
| الإضافة 2 — واجهة /trade | ✅ منفّذة بالكامل (معاينة متبادلة + عناصر + مال + موافقة الطرفين) |
| الإضافة 3 — MySQL/MariaDB/Redis | 📋 خطة تفصيلية فقط كما طُلب: `docs/DB_SCALING_PLAN.md` |
| البناء | ✅ `compileJava` ناجح (Java 25 / Gradle 9.5.1) |
| الاختبارات | ✅ 309+ اختبار ناجح، منها ~25 اختباراً جديداً |

**قرار الإصدار (مُحدّث بقرار المالك 2026-09-06)**: الترقيم النهائي هو
`2.1.4` داخل عائلة `2.1.x` — الإضافات (أوامر `/trade` والمزايدة) **إضافية
بحتة** (additive) ولا تكسر توافق الماودات المرافقة بفضل آلية `fromCode`
الآمنة، لذا لا تستدعي قفزة عائلة. كانت قد رُقّمت مؤقتاً `2.2.0` ثم أُعيد
الترقيم بقرار المالك؛ عائلة `2.2.x` **محجوزة** حصرياً لعصر المالتي
سيرفر/إعادة هيكلة التخزين القادمة (انظر `VERSIONING.md`).

---

## 2) قرارات تصميم مهمة ولماذا

### أ) الضمان (Escrow) بحساب نظامي وليس "خصم عند الفوز"
- **المشكلة**: لو خُصم المال عند الفوز فقط، قد يفوز لاعب لا يملك المال →
  فشل البيع وعقاب للبائع (نصب عكسي).
- **الحل المتبع**: الخصم فور المزايدة إلى حساب ضمان نظامي (`EscrowAccount`
  — UUID صفري)، والإرجاع الفوري عند التجاوز/الإلغاء/الشراء الفوري.
- **لماذا هذا هو المعيار**: نفس نموذج Crazy Auctions وAuctionHouse — طلب
  المستخدم صراحة "غش من الإضافات الأخرى".
- **كل حركة مال** تمر عبر `transferAtomicWithLedger` (آلية 2.1.3 المتصلبة)
  فتكون كل حركة ذرية مع دليلها في السجل داخل نفس المعاملة.

### ب) حالة المزايدة في جدول منفصل وليس أعمدة جديدة
- `auction_bid_state` منفصل عن `auction_listings` → `AuctionEntry` record
  لم يتغير → **صفر كسر** للاختبارات والمستهلكين الحاليين.
- قائمة بلا صف حالة مزايدة = قائمة buy-now عادية كما كانت تماماً.

### ج) التداول بحاويتين متطابقتين (mirrored) وليس حاوية مشتركة
- كل لاعب يرى عناصره يساراً وعناصر شريكه يميناً (نمط TradeMe).
- المرآة تتزامن يدوياً + `broadcastFullState()` بعد كل تغيير (ضمان PR#13
  ضد الأشباح يمتد لنافذة التداول).

### د) جلسة التداول خالية من كائنات اللاعب
- `TradeSession` تحمل UUIDs وأسماء فقط، والمدير يفكّ اللاعب الحي عند الحاجة.
- السبب العملي: Mockito لا يستطيع محاكاة `ServerPlayer` (لا يمكن تهيئة
  تسلسلها خارج بيئة اللعبة) — فأعدت التصميم ليكون قابلاً للاختبار الوحدوي.
- فائدة إضافية: لا مراجع معلقة (stale) بعد انقطاع الاتصال.

### هـ) مال التداول يمر عبر `transferOffline` الحالي
- يحصل مجاناً على: حمولات Governance (allowTransfer/afterTransfer)، حدود
  التحويل، الضرائب — دون إضافة أي واجهة hooks جديدة.
- السلوك المقصود: تداول المال يُحتسب ضمن حدود التحويل اليومية.

---

## 3) المخاطر المتبقية والنوافذ النظرية (بصراحة)

1. **نافذة crash بين خصم الضمان وتسجيل المزايدة** (ملي ثوانٍ): لو انهار
   الخادم بين الخطوتين، المال يبقى في حساب الضمان مع صف `BID_PLACED` في
   السجل دون تغيير حالة المزايدة. الفحص الدوري عند الإقلاع
   (`checkEscrowConsistency`) ينبه بأي فرق — **يُصحّح يدوياً** عبر السجل.
   اعتبرت أن إضافة آلية ترحيل كاملة لهذه النافذة الضئيلة لا تستحق التعقيد
   في هذا الإصدار (نفس مستوى تحمّل النوافذ الذي كان في المشروع قبل 2.1.3).

2. **عناصر جلسة التداول في الذاكرة** (نمط SellContainer نفسه): لو انهار
   الخادم **أثناء** نافذة تداول مفتوحة، العناصر المعروضة ضاعت (لم تُحفظ).
   نفس التعرض الموجود أصلاً في واجهة البيع — وثّقته لأن نافذة التداول أطول.
   العلاج المستقبلي: حفظ الجلسات في DB عند كل تغيير (جداول trade_session /
   trade_items) — سهل الإضافة لاحقاً بنفس أنماط الكود الحالية.

3. **سعة حساب الضمان**: `MAX_BALANCE = 100,000,000` يطبّق على حساب الضمان
   أيضاً؛ لو تراكمت مزايدات ضخمة جداً بشكل متزامن (>100M مجموعاً) سيرفض
   الضمان برسالة واضحة للاعب. غير واقعي عملياً، لكنه موثّق هنا وفي اختبار
   `escrowOverflowProtected`.

4. **عناصر التداول بلا Blacklist**: أي آيتم يمكن عرضه في التداول (بما فيها
   shulkers بداخلها ما تشاء). إن أردت منع نشر آيتمات معينة أضف قائمة سوداء
   في `TradeScreenHandler.handleMyOfferSlotClick` — نقطة مركزية واحدة.

5. **حقل `itemNbt` في `storeWonItemRowFromDelivery` يُخزن NULL** عند فشل
   التسليم المباشر للفائز الذي انقطع بين اللقطة والتسليم — يستعاد العنصر
   من المادة الأساسية فقط عند `/ah collect` (قد يفقد enchanments في هذه
   الحالة الاستثنائية الضيقة جداً). حالات أخرى تحفظ NBT كاملاً.

6. **Chat prompt يعتمد `ServerMessageEvents.ALLOW_CHAT_MESSAGE`** من fabric
   message API v1 — اختبرته بالترجمة فقط، وليس بسيرفر حي. إن اختلفت سلوكيات
   توقيع الرسائل في 26.1.x فالأمر معزول في `chat/ChatPrompts.java` فقط.

---

## 4) اختبار قبول مقترح على سيرفر حي (سريع)

**المزايدة:**
1. لاعبان A وB. A: `/ah sell 5000 500` → يجب ظهور BIDDING ENABLED في GUI.
2. B ينقر يميناً على العنصر → يغلق GUI ويطلب المبلغ في الشات. اكتب `600`.
3. تحقق: رسالة نجاح، `/balance` انخفض 600، `/transactions` يظهر BID.
4. A يشتري buy-now الآن → يصل B إشعار استرداد 600 فوراً (`BID+` في السجل).
5. كرر ودع المزاد ينتهي بمزايدة B → B يستلم العنصر (أو عبر `/ah collect`
   لو كان أوفلاين) وA يستلم 600.
6. تجاوز سريع في آخر 10 دقائق → يجب تمديد الوقت 5 دقائق.

**التداول:**
1. لاعبان متجاوران: `/trade B` ثم B: `/trade accept` → نافذتان.
2. A يضع آيتم + يضبط مالاً 100 (زر الذهب ثم الكتابة في الشات).
3. B يرى التحديث فورياً يميناً. كلاهما READY → التنفيذ فوري.
4. تحقق: تبادل العناصر، تحويل 100، صفوف `TRD-`/`TRD+` في `/transactions`.
5. بينما الجلسة مفتوحة: أي تعديل من A بعد ضغط READY → يلغي جاهزية الطرفين.
6. افصل اتصال A أثناء الجلسة → تعود عناصر B إليه فوراً.
7. جرب ESC أثناء الجلسة → إلغاء وعودة العناصر (لا شيء يقع أرضاً).

---

## 5) ملاحظات صيانة لاحقة

- **الثوابت القابلة للضبط** كلها في `BidRules` و`TradeManager` و`ChatPrompts`
  (النسب، النوافذ، المسافة). أفضل خطوة تالية: نقلها إلى `storage.json` مع
  hot-reload مثل `listingFee`.
- **إضافة أنواع سجل جديدة مستقبلاً**: لا تنسَ تحديث switchين في
  `TransactionsCommand.formatTransactionEntry` (الترجمة ستفشل بوضوح إن نُسيت
  — الـ switch مغطي بالكامل).
- **`getMaterialName`** يرجع UPPERCASE — أسماء العناصر في رسائل التداول
  تتبع نفس عرف المشروع.
- **اختبارات مستقبلية**: انسخ نمط `BidEscrowFlowTest` (SQLiteStorage فقط)
  لأي منطق مال جديد، ونمط `TradeSessionStateTest` (دومين نقي) لأي حالة
  جلسة جديدة. تجنّب أي test يلمس `ItemStack`/`Items` مباشرة — تتطلب
  bootstrap اللعبة.
- **الضمان وbaltop**: إن أضفت أوامر إحصاء/ترتيب جديدة، استثنِ
  `EscrowAccount.UUID_ZERO` منها (نفس نمط `getTopBalances`).

---

## 6) ملفات جديدة/معدلة (مرجع سريع)

**جديدة:**
```
src/main/java/com/solidus/auction/BidRules.java        قواعد المزايدة النقية
src/main/java/com/solidus/auction/BidState.java        سجل حالة المزايدة
src/main/java/com/solidus/economy/EscrowAccount.java   حساب الضمان النظامي
src/main/java/com/solidus/chat/ChatPrompts.java        مطالبات الشات (مزايدة/مال)
src/main/java/com/solidus/trade/TradeManager.java      منسق التداول
src/main/java/com/solidus/trade/TradeSession.java      آلة حالة الجلسة
src/main/java/com/solidus/trade/TradeContainer.java    حاوية الجلسة
src/main/java/com/solidus/trade/TradeGUI.java          تخطيط النافذة
src/main/java/com/solidus/trade/TradeScreenHandler.java معالج النقرات
src/main/java/com/solidus/commands/TradeCommand.java   أمر /trade
src/test/java/com/solidus/auction/BidEscrowFlowTest.java
src/test/java/com/solidus/trade/TradeSessionStateTest.java
docs/FEATURES_TRADE_BIDDING.md    التوثيق الرسمي للميزتين
docs/DB_SCALING_PLAN.md           خطة MySQL/MariaDB/Redis (الإضافة 3)
notes/AGENT_NOTES.md              هذا الملف
```

**معدلة:**
```
SolidusMod.java            ربط ChatPrompts + TradeManager + تنظيف الانقطاع
AuctionManager.java        جداول المزايدة + placeBid + تسوية الفوز + السواتب
AuctionGUI.java            عرض معلومات المزايدة + attachBidStatesAndBuild
AuctionScreenHandler.java  النقر الأيمن → مطالبة المزايدة
AuctionCommand.java        /ah bid + /ah sell <price> <startbid>
TransactionLog.java        الأنواع الجديدة BID_*/AUCTION_WON/TRADE_*
SQLiteStorage.java         استثناء الضمان من baltop (+fallback)
EconomyEngine.java         إنشاء حساب الضمان برصيد صفر
BalanceManager.java        (لم يتغير فعلياً — الفحص فقط)
SolidusPermissions.java    AUCTION_BID + TRADE
TransactionsCommand.java   تغطية الأنواع الجديدة في switch
PacketHandler.java         توجيه نقرات TradeScreenHandler + hasSolidusScreenOpen
gradle.properties          mod_version = 2.1.4
README.md                  أوامر جديدة في جدول الأوامر
```

---

## 9) ملاحظات إصدار 2.2.0 — مرحلتا التخزين (DB Scaling Phase 1+2)

**ما نُفّذ (2.1.5 ثم 2.2.0)**: واجهة `StorageBackend` (refactor نقي، الـ 326
اختبار بقت خضراء)، ثم `MySqlStorage` كامل: HikariCP، `DECIMAL(18,2)` مع غلاف
`Money` (BigDecimal)، أقفال `FOR UPDATE` بترتيب UUID التصاعدي الحتمي، إعادة
محاولة deadlock (1213/1205) مرتين مع backoff، تحديثات CAS، صفوف الـ ledger داخل
معاملة التحويل، قراءات database-first (الكاش fallback فقط — لا يظهر رصيد قديم
بين سيرفرين)، فشل مغلق عند بدء التشغيل بدون قاعدة بيانات، `TransactionLog`
مدرك للهجتين (DDL MySQL + اتصالات pooled)، `storage.json` مع تجاوز كلمة المرور
من `SOLIDUS_DB_PASSWORD`، وملف `docs/sql/mysql/001_init.sql` (يشمل جداول
المزادات الاستباقية).

**قرارات جوهرية**:
1. الأنواع المتداخلة (`TransferOutcome`...) بقيت على `SQLiteStorage` عمداً —
   `SolidusAPI` يعرضها للماودات المرافقة وقاعدة المالك تمنع كسرها في عائلة
   patch. الاستخراج للـ top-level مؤجل لقفزة عائلة (غير ضروري للواجهة).
2. الكاش في وضع MySQL ليس مصدراً أبداً — كل قراءة رصيد تلمس القاعدة (بشبكة،
   كاش قديم = ثغرة ازدواج). Redis L1/L2 في 2.2.1 هو من سيحسن القراءة بأمان.
3. فشل الاتصال عند الإقلاع = توقف واضح (fail closed) — سيرفر شبكة لا يجب أن
   يقلع باقتصاد خاطئ؛ وفشل القراءة أثناء التشغيل = قيمة الكاش (لا صفر أبداً).

**حدود النطاق الصادقة (موجودة أيضاً في DB_SCALING_PLAN §11)**:
- المزادات ما زالت SQLite محلية في 2.2.0 (أرجل المال تمر عبر القاعدة المشتركة
  أصلاً) — نقل `AuctionStore` = 2.2.1.
- أمر `/solidus-admin storage migrate` غير منفذ — الترحيل اليدوي SQL حتى 2.2.1.
- جدول `operations` منشأ لكن غير موصول بالعمليات بعد (2.2.1 مع Redis).
- اختبارات MySQL الحقيقية (عقد + سباق تزامن بين نسختين) محمولة على CI وتُتخطى
  ذاتياً بدون `SOLIDUS_TEST_MYSQL_HOST` — البنية هنا بلا Docker؛ يجب تشغيلها
  مرة واحدة على CI أو ضد MariaDB حقيقية قبل الإنتاج (قائمة القبول أدناه).

**قائمة قبول مقترحة قبل تشغيل شبكة إنتاجية**:
- [ ] CI مع خدمة MariaDB 10.11 → `SOLIDUS_TEST_MYSQL_HOST` → العقد + السباق أخضر.
- [ ] تجربة سيرفرين محليين على نفس القاعدة: دفع متقاطع + /baltop موحد.
- [ ] ترحيل بيانات تجريبي (SQLite → MySQL يدوياً) ومطابقة `SUM(balance)` للسنت.
- [ ] جدار ناري: قاعدة البيانات على شبكة خاصة فقط؛ TLS عبر `useSsl=true`.

---

## 10) ملاحظات إصدار 2.2.1 — مزادات الشبكة + طبقة Redis اختيارية + أمر الهجرة

**نطاق الإصدار (بنود القسم 11 من خطة DB_SCALING — الخمسة كلها):**

1. **منفذ المزادات إلى قاعدة الشبكة**: `AuctionManager` صار متوافقاً مع اللهجتين
   تماماً كـ `TransactionLog` — متغير داخلي `AuctionDialect` (SQLITE | MYSQL) +
   مساعد `withAuction` يلفّ حوالي 30 موقع SQL. في وضع SQLITE يبقى ملف
   `auctions.db` لكل سيرفر كما هو (سلوك مضمون للاختبارات الـ344 السابقة)، وفي
   وضع MYSQL تعيش جداول المزادات في نفس قاعدة الاقتصاد — سوق واحد موحد للشبكة.
   أمان تعدد السيرفرات مبني على المُطالبات الشرطية (exactly-once) الموجودة
   أصلاً + `FOR UPDATE SKIP LOCKED` على استعلام المسح الدوري (معاملة قصيرة،
   مع تراجع دائم للاستعلام العادي على إصدارات أقدم من MariaDB 10.6 / MySQL 8).
   المعاملات اليدوية (`BEGIN IMMEDIATE`) بقيت لـ SQLite، وMySQL يستخدم واجهة
   JDBC (`setAutoCommit(false)`).

2. **طبقة Redis (اختيارية، معطلة افتراضياً)**: `RedisLayer` على Lettuce 6.5.5.
   - ذاكرة L2 للأرصدة (TTL افتراضي 30 ثانية) + قناة إبطال `solidus:bal:inv`
     + قناة أحداث `solidus:events` لتسليم الإشعارات فورياً عبر الشبكة (السيرفر
     الذي يستضيف اللاعب يسلّم ويحذف الصف الدائم — لا تكرار عند الدخول).
   - **قاعدة حاسمة**: MySQL يبقى المصدر الوحيد للحقيقة — الكتابة لا تنشر رصيداً
     كحقيقة بل تحذف المفتاح وتبثّ الإبطال؛ TTL هو صمام الأمان لرسائل pub/sub
     المفقودة؛ وعمليات المال لا تثق أبداً بقيمة مخزنة مؤقتاً (إعادة تحقق ذرية
     داخل معاملة SQL).
   - قاطع دائرة: 3 فشلات متتالية → تخفيض لقراءات قاعدة البيانات فقط لمدة 30
     ثانية ثم إعادة محاولة تلقائية. انقطاع Redis لا يُفشل أي عملية مالية.

3. **أمر الهجرة `/solidus-admin storage migrate`** (OP 4، صلاحية
   `solidus.command.admin`): `StorageMigrator` ينسخ الأرصدة + دفتر العمليات +
   الإشعارات + جداول المزادات الخمسة بترحيل مفتاحي (keyset pagination) وكتابات
   idempotent (جداول الحالة/المال ON DUPLICATE KEY UPDATE — الأحدث يفوز؛
   الجداول الإلحاقية INSERT IGNORE بمعرفات صريحة)، ويتحقق من العدد
   والمجموع `SUM(balance)` للسنت، ويرفض اللاعبين المتصلين بدون `--force`،
   ويكتب تقريراً في `config/solidus/`. التحويل الفعلي = قلب الإعداد إلى
   `mysql` + إعادة تشغيل؛ ملفات SQLite تبقى نسخة الاسترجاع.

4. **ربط جدول operations**: طريقة جديدة `transferAtomicWithLedger(opId,
   opType, …)` في الواجهة (default = تفويض، صفر تغيير سلوكي لأي رفيق) مع
   تنفيذ حقيقي في MySqlStorage بالنمط المصحح من مراجعة الخطة (§3): claim عبر
   upsert (وليس INSERT عارض)، تمييز المالك بقيمة affected-rows (1 = أدخلنا /
   2 = سبقنا)، ثم تسجيل النتيجة وإعادتها عند إعادة التشغيل. لا يوجد مستدعٍ
   إنتاجي يمرر opId بعد — العهدة موجودة لجسور تعدد السيرفرات، والاختبار
   `MySqlOperationsIdempotencyTest` يثبت الضمانة.

5. **SKIP LOCKED للمسح الدوري** — نفّذ (بند 1 أعلاه). أما الـ outbox الكامل
   فقد بقي مؤجلاً بحسب قياس الحاجة (قرار المراجعة §4): جدول
   `pending_notifications` يؤدي دور مخزن الأحداث الدائم عملياً.

**ثلاثة أخطاء حقيقية اكتُشفت أثناء المنفذ وأُصلحت في 2.2.1:**

1. `markExpiredRowCollectible` لم يربط معامل `?` إطلاقاً في 2.1.x — SQLite
   تعامله كـ NULL فلا يطابق أي صف: مسار "عناصر البائعين غير المتصلين تصبح
   قابلة للجمع" كان معطلاً فعلياً (العناصر لا تعود إلا إذا صادف أن البائع
   متصل أثناء المسح). أُصلح الربط.
2. `checkEscrowConsistency` كان يقرأ `player_balances` عبر اتصال المزادات —
   ملف SQLite مختلف لا يحتوي الجدول — فيفشل الفحص بصمت عند كل إقلاع. الآن
   يُقرأ رصيد الضمان من واجهة التخزين (غير حاجب).
3. `extensions_used` لم يكن يُحدَّث أبداً — سقف مانع السنابينغ (12) كان حبراً
   ميتاً؛ كل تمديد يزيد العداد الآن فتعود القاعدة أرضية.

**قرارات تصميم تستحق التذكّر:**
- بنية `withAuction` المطابقة لـ `withConnection` في TransactionLog (نفس
  النمط المثبت في 2.2.0) بدل تجريد `AuctionStore` كامل — تغيير أقل، اختبارات
  قديمة سليمة، وثغرات أقل.
- `MEDIUMTEXT` لأعمدة item_nbt في MySQL (JSON عناصر معقدة قد يتجاوز حد TEXT
  64KB) — وُثّق في `001_init.sql` وفي DDL المضمن.
- إشعارات Redis تُبَثّ فقط بعد نجاح إدراج الصف الدائم (وليس قبله) — لا يوجد
  نافذة إشعار مفقود؛ الأسوأ أن التسليم الفوري يفشل فيبقى مسار الدخول.
- اختبارات جديدة: `StorageConfigRedisTest` (6 وحيدية)، `MySqlAuctionDialectTest`
  (5 بوّابية)، `MySqlOperationsIdempotencyTest` (4 بوّابية)، `StorageMigratorTest`
  (1 بوّابية)، `RedisLayerTest` (4 بوّابية) — المجموع 364 تنفيذاً: 337 نجح /
  0 فشل / 27 تخطياً ذاتياً محلياً.

**قائمة قبول قبل شبكة إنتاجية (إضافات 2.2.1):**
- [ ] تشغيل `MySqlAuctionDialectTest` + `MySqlOperationsIdempotencyTest` +
      `StorageMigratorTest` ضد MariaDB حقيقية عبر `SOLIDUS_TEST_MYSQL_HOST`.
- [ ] تشغيل `RedisLayerTest` ضد Redis حقيقي عبر `SOLIDUS_TEST_REDIS_URI`.
- [ ] سيناريو مزدوج السيرفر: مزاد معروض من سيرفر يُشترى من آخر؛ مزايدة من
      سيرفرين بذات اللحظة (المُطالبة الشرطية تضمن مُزايداً واحداً).
- [ ] `/solidus-admin storage migrate` على نسخة تجريبية + مطابقة التقرير.
- [ ] تجربة انقطاع Redis أثناء حركة مالية: صفر أخطاء مالية، تخفيض للقراءة فقط.

---

## 11. ملاحظات 2.2.2 — منصة الاختبار الكونسولية (اختبار اقتصاد كامل بدون لاعبين حقيقيين)

**الطلب:** إضافة أوامر تسمح بقيادة تحويلات بيع وشراء ومزادات بين حسابات وهمية من كونسول
السيرفر فقط، مع أي إضافات متقدمة تخدم اختبارات أفضل.

**ما نُفذ:**

- **`AdminOps` (حزمة جديدة `com.solidus.admin`)** — خدمة منطقية نقية خالية من
  ServerPlayer: كل العمليات تعالج الحسابات بـ UUID+اسم فقط. Brigade shell رفيع في
  `SolidusAdminCommand` يفوض إليها — نفس نمط TradeSession المتبع منذ 2.1.4.
- **هوية الحساب الوهمي:** `UUID.nameUUIDFromBytes("OfflinePlayer:" + name)` — اشتقاق
  offline-mode الرسمي للفانيلا. على سيرفر offline-mode، لاعب حقيقي بنفس الاسم يرث
  الحساب (ميزة اختبار، وتحذير للإنتاج موثق في الوثائق). تحقق الاسم بقاعدة
  `[A-Za-z0-9_]{1,16}`.
- **الأوامر:** `account create|balance|list`، `money give|set|take`، `pay-as`،
  `bid-as`، `auction create`، `audit`، `diag` — كلها تعمل من الكونسول/Rcon.
- **سلامة المال:** `pay-as` يمر عبر `transferOffline` (نفس مسار /pay: هوكات
  Governance، حدود، ذرّية كل-أو-لا شيء) ويسجل `PAY_SEND/PAY_RECEIVE` بمطابقة
  PayCommand. `give/set/take` عبر نفس البدائيات مع أنواع سجل جديدة `ADMIN_GIVE/SET/TAKE`
  (حدّثت switchي `TransactionsCommand` — المترجم يفرض الاكتمال). `bid-as` و`auction
  create` عبر مساري escrow وlisting الحقيقيين.
- **إعادة هيكلة AuctionManager (الحفاظ على السلوك):** `placeBid(ServerPlayer)` أصبح
  غلافاً فوق `placeBidAs(UUID, name, ...)` بنفس المراحل الثلاث حرفياً؛ `sendBidError`
  استُخرج إلى `bidErrorMessage` ثابت (نصوص بدل Components — يبقي AdminOps خالياً من
  كلاسات ماينكرافت). جديد: `listItemAs(UUID, name, ItemStack, ...)` — نفس مسار رسوم
  القائمة والهوكات والسجل، بدون لمس مخزون (العنصر مُستدعى إدارياً للبوت).
- **`audit`:** فحص ثوابت — مسح الأرصدة السالبة عبر صفحات baltop من الذيل (الأرصدة
  مرتبة تنازلياً فالسالب لاحقة بالضرورة)، سلامة escrow (لا سالب أبداً)، لقطة عرض
  المال وGini للمقارنة بين الجولات. **`diag`:** أي backend فعّال، وضع متجر المزاد،
  حالة Redis، حجم كاش الأسماء.
- **CI (تغيير مهم):** `test.yml` أصبح يشغّل حاويتي خدمة `mariadb:11` + `redis:7` مع
  متغيرات `SOLIDUS_TEST_*` — الاختبارات الـ27 البوّابية (سباق التحويلات 200 عملية من
  نسختين، idempotency، لهجة المزاد MySQL، المهاجر بقاعدة مؤقتة، طبقة Redis) ستنطلق
  حقيقية عند كل push. المهاجر يستخدم root لأنه ينشئ قواعد مؤقتة (CREATE/DROP DATABASE).
- **اختبارات جديدة:** `AdminOpsTest` (23 حالة) بهارنس SQLiteStorage حقيقي في مجلد مؤقت.

**قرارات جديرة بالتسجيل:**

- تعذّر استخدام Mockito في هذا المشروع على سلسلة أدوات Java 25 (انفجار
  retransform في inline mock maker) — حل الأرشفة: fake فرعي لـ EconomyEngine يُوصل
  البدائيات الحقيقية بدلاً من محاكاتها. لا Mockito في ملف الاختبار إطلاقاً.
- `audit` يقرأ escrow عبر `getBalance` التي تُنشئ الصف تلقائياً عند غيابه — في الإنتاج
  هذا لا يحدث أبداً (الصف يُنشأ مسبقاً بصفر في `EconomyEngine.initialize`)، واختبارنا
  يطابق ذلك (أنشأنا escrow في setUp). لو تغير هذا الثابت يوماً فسينضج رصيد وهمي —
  وثّقته في تعليق الاختبار.
- `pay-as` يشترط وجود الحسابين مسبقاً (بدل upsert الضمني) — يبقي اختبارات حفظ كمية
  المال حتمية بلا "رصيد بداية" خفي للمستقبل الجديد.
- `bid-as`/`auction create` يرجعان "accepted into pipeline" فوراً والنتيجة التفصيلية
  تصل عبر feedback sink (آمن خيطياً عبر server.execute) — نفس تفويض المهام للخوادم
  في باقي الكود.

**قائمة قبول 2.2.2:**

- [x] 387 تنفيذ اختبار: 360 نجح / 0 فشل / 27 بوّابية محلية.
- [ ] مراقبة أول تشغيل CI على GitHub Actions (المفروض 0 تخطٍ هذه المرة) — أي فشل
      هناك = مشكلة تهيئة حاويات وليست منطق مال.
- [ ] تجربة سيناريوهات `docs/CONSOLE_TESTING.md` §3.3/§3.4 على شبكتك الحقيقية
      (سيرفران + MariaDB) — الأمران الجاهزان: `audit` ثم `diag`.

---

## 12) ملاحظات 2.2.3 — أول تشغيل CI حقيقي يكشف 5 أخطاء كامنة (3 منها إنتاجية خطيرة)

هذا الإصدار هو **المكافأة العملية لقاعدة "لا يثق باختبار لم يُشغَّل فعلاً"**:
اختبارات الـ27 البوّابية في 2.2.1/2.2.2 كانت تُتخطى ذاتياً محلياً (لا Docker
ولا خادم MySQL في بيئة العمل)، وأول دفعة فعلي على حاويات `mariadb:11` +
`redis:7` في CI رجعت بـ4 فشلات. حاكيت البيئة محلياً (MariaDB 11.8.6 محمولة
بدون root: حزم .deb مستخرَجة + `mariadbd` على المنفذ 3307) حتى أشخّص وأصلح
وأتحقق قبل إعادة الدفع — فانكشفت أثناء ذلك **مشكلة خامسة** لم يلتقطها CI أصلاً.

**الأخطاء الخمسة (التفاصيل الكاملة في DB_SCALING_PLAN §11):**

1. **دفع مزدوج عند إعادة المحاولة (أخطرها):** claim جدول `operations` كان
   `INSERT … ON DUPLICATE KEY UPDATE op_type = VALUES(op_type)` مع فصل على
   `affected-rows 1 مقابل 2` — لكن MariaDB تعيد **0** للتحديث الذي لا يغيّر
   شيئاً، وإعادة المحاولة تمرر نفس `op_type` فلا يغيّر شيئاً → تجاوز فرع
   "غير المالك" ونُفِّذ التحويل **مرة ثانية**. الحل: `INSERT IGNORE`
   (1 = مطالبة جديدة، 0 = الصف موجود) — دلالات لا لبس فيها على المحركين.
2. **كنس الأيتام أرشف صفراً على MySQL:** نداء `insertSoldHistory` في
   `recoverOrphanedSoldRows` نسّي تمرير `dialect` → ولّد `INSERT OR IGNORE`
   (SQL غير صالح على MariaDB) → كل يتيم مطابق للسجل يقع في فرع إعادة العرض.
   على شبكة حقيقية كان هذا **يعيد عرض عناصر مدفوعة** (مضاعفة عناصر).
3. **البحث في المزاد يرجع قائمة فارغة دائماً على MySQL:** `ESCAPE '\'` —
   SQLite تعامل الشرطة المائلة حرفياً بينما MariaDB/MySQL تعالجها كحرف تهريب
   داخل السلاسل → تحليل العبارة ينكسر والاستثناء يُبتلع في قائمة فارغة.
   الحل: حرف تهريب `!` (حرفي على المحركين) مع تهريب `!→!!` و`%→!%` و`_→!_`.
4. **المهاجر يفشل على قاعدة هدف جديدة:** كان ينشئ جداول المزاد الخمسة فقط
   ويعتمد "الإنشاء التلقائي من MySqlStorage" الذي لا يعمل أبداً في مساره —
   `player_balances` غير موجود. الآن ينشئ المخطط الكامل عبر
   `MySqlStorage.createEconomySchema` + جداول السجل والإشعارات، ويتسامح مع
   جداول ناقصة في نسخ SQLite قديمة (تتخطى بملاحظة صريحة في التقرير).
5. **(كامن منذ 2.2.1، انكشف أثناء التحقق المحلي) — مرحلة تحقق المهاجر تفقد
   الاتصال:** كتل `try (Connection sc = sqlite; …)` كانت تغلق اتصال SQLite
   المشترك بعد أول كتلة تحقق فتنهار بقية العدّادات بـ"database connection
   closed". لم يظهر يوماً لأن مرحلة النسخ كانت تفشل أولاً دائماً.

**قرارات جديرة بالتسجيل:**

- Mockito مكسور على Java 25 حتى **على مستوى الواجهات** في هذه البيئة (ليس
  الصفوف فقط) — اختبار مسارات القراءة أصبح stubs خالصة بلا Mockito نهائياً
  (تنفيذ مجهول كامل لـ`StorageBackend` + fake فرعي لـ`EconomyEngine`).
- توقع عدّ السجل في اختبار الأيتام كان 2 والصحيح 1 (اليتيم المُعاد عرضه لا
  يُؤرشف) — صُحّح بالاستناد إلى الاختبار الشقيق SQLite في
  `AuctionSettlementHistoryTest.relistsOrphanWithoutLog` كمرجع سلوك.
- اختبار المهاجر يهيئ `ConfigManager` على مجلد مؤقت (نمط StorageConfigTest)
  حتى يكون تأكيد وجود ملف التقرير حقيقياً لا اعتباطياً.
- مصدر SQLite الاختيري في اختبار المهاجر اكتمل بالجداول الخمسة للمزاد + صفوف
  فعلية (sold_history/bid_state/won_items) ليختبر النسخ الكامل والتقارب عند
  إعادة التشغيل بدل الاعتماد على منطق التخطي.

**قائمة قبول 2.2.3:**

- [x] MariaDB 11.8.6 محلية (بلا Docker): كل الـ23 اختبار MySQL البوّابية
      ناجحة ضد خادم حقيقي — 387 تنفيذ: 383 نجاح / 0 فشل / 4 تخطي (Redis فقط).
- [x] مراقبة تشغيل CI بعد الدفع: **مكتملة — أخضر بالكامل، صفر تخطٍ**.
      الرسمي عبر API (run 34047523654، تزامن 2026-09-06T17:05Z على 1a0009e):
      Tests = success، CodeQL = success، حاويات الخدمة اشتغلت فعلاً
      (mariadb:11 + redis:7 pulled and healthy)، سجل المهمة (job
      101525136185): 774 PASSED / 0 FAILED / 0 SKIPPED، وكل خطوات
      المهمة الـ16 ناجحة بلا تخطٍ.
- [x] لم يظهر أي فشل — لا حاجة لفرضية «اختلاف بيئة الحاويات». الأرقام
      المحلية والمحلية في CI متطابقة في الدلالة: منطق المال سليم على
      المحركين. **قائمة قبول 2.2.3 مغلقة بالكامل.**
